{
  "name": "homepage",
  "version": "1.0.0",
  "private": true,
  "description": "My résumé.",
  "scripts": {
    "build": "node build/render.js && node build/render-ats.js",
    "verify": "node build/verify.js"
  }
}
